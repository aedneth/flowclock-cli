## Summary

<!-- What does this change and why? -->

## Checklist

- [ ] `npm run build && npm test && npm run lint && npm run typecheck` pass
- [ ] Added/updated tests (pure logic in `src/lib/**` is unit-tested)
- [ ] Updated `CHANGELOG.md` under `Unreleased`
- [ ] Preserves the HUD UX (single centered line, invisible controls, no chrome)
- [ ] Any new command/flow has a `--json` form and a deterministic exit code

## Notes for reviewers

<!-- Anything that needs extra eyes? -->
